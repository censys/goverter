input:
    input.go: |
        package execution

        // goverter:converter
        type Converter interface {
            // goverter:struct:assign:source Get.*
            Convert(source Input) Output
        }

        type Input struct{ name string }
        func (i Input) GetName() string { return i.name }

        type Output struct{ Name string }
error: |-
    error parsing 'goverter:struct:assign:source' at
        @workdir/input.go:6
        func (github.com/jmattheis/goverter/execution.Converter).Convert(source github.com/jmattheis/goverter/execution.Input) github.com/jmattheis/goverter/execution.Output

    source regex "Get.*" must contain at least one capture group to extract the field name
