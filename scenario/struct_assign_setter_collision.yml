input:
    input.go: |
        package execution

        // goverter:converter
        type Converter interface {
            // goverter:struct:assign field method
            Convert(source Input) Output
        }

        type Input struct { Name string }
        type Output struct { Name string }
        func (o *Output) SetName(name string) { o.Name = name }
error: |-
    Error while creating converter method:
        @workdir/input.go:6
        func (github.com/jmattheis/goverter/execution.Converter).Convert(source github.com/jmattheis/goverter/execution.Input) github.com/jmattheis/goverter/execution.Output
            [source] github.com/jmattheis/goverter/execution.Input
            [target] github.com/jmattheis/goverter/execution.Output

    | github.com/jmattheis/goverter/execution.Input
    |
    source.
    target.SetName
    |      |
    |      | string
    |
    | github.com/jmattheis/goverter/execution.Output

    Multiple target members cover output field "Name".

    Set goverter:struct:assign:prefer to "field" or "method" to choose which one wins, or disambiguate with an explicit goverter:map or goverter:ignore.
